name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22.2"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: src/app/package-lock.json

      - name: Install server dependencies
        working-directory: src/server
        run: go mod download

      - name: Install app dependencies
        working-directory: src/app
        run: npm ci

      - name: Lint app
        working-directory: src/app
        run: npm run lint

      - name: Test server
        working-directory: src/server
        run: go test ./...

      - name: Test app
        working-directory: src/app
        run: npm test -- --runInBand

  build:
    needs: lint-and-test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22.2"

      - name: Build server
        working-directory: src/server
        run: go build -o bin/server ./cmd/server

  e2e-web-tests:
    needs: lint-and-test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: src/app/package-lock.json

      - name: Install dependencies
        working-directory: src/app
        run: npm ci

      - name: Install Playwright browsers
        working-directory: src/app
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests
        working-directory: src/app
        run: npm run test:e2e:web

      - name: Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: src/app/playwright-report/
          retention-days: 7

  # ============================================================
  # E2E 测试 Job（占位配置，暂未启用）
  # ============================================================
  # 注意：此 Job 需要本地安装 Maestro 和模拟器环境才能运行。
  # 当前不依赖 GitHub Actions，需要在本地手动执行 E2E 测试。
  # 运行方式：
  #   1. 安装 Maestro: brew tap mobiledevops/mobiledevops && brew install maestro
  #   2. 启动 iOS/Android 模拟器
  #   3. 启动应用: cd src/app && npm start
  #   4. 运行测试: cd src/app && npm run test:e2e
  #
  # e2e-tests:
  #   needs: build
  #   runs-on: macos-latest
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     - name: Setup Maestro
  #       run: brew tap mobiledevops/mobiledevops && brew install maestro
  #     - name: Run E2E tests
  #       working-directory: src/app
  #       run: npm run test:e2e
  # ============================================================
