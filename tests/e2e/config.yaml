# Maestro E2E 测试配置
# 文档: https://maestro.mobile.dev/

# Expo Go 应用 ID（在 Expo Go 中运行时使用）
# 本地开发时使用 Expo Go，生产环境需要修改为实际应用 ID
appId: host.exp.exponent

# 测试环境配置
onFlowStart:
  - clearState

onFlowComplete:
  - stopApp
