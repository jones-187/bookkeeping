# 集成测试

最后更新：2026-03-27

此目录捕获集成测试意图。

## 当前现状

尚无单独的集成套件。当前脚手架通过以下方式验证：

- 应用 UI 行为的 Jest 测试
- 服务端 HTTP 测试
- 手动本地启动检查

## 近期集成范围

第一个真正的集成套件应覆盖：

- 应用到服务端的 bootstrap 连接性
- 基于环境的 API 基础 URL 配置
- 服务端启动和端点响应

## 后续集成范围

一旦持久化和同步存在，添加针对以下内容的集成测试：

- SQLite 读写
- 迁移行为
- 同步请求幂等性
- 冲突处理
